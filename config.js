/* ===========================================================
   이 파일 한 줄만 고치면 됩니다.

   · 비워두면  → 각자 API 키를 넣는 방식으로 동작합니다.
   · 주소를 넣으면 → 구글 로그인 방식으로 바뀝니다.

   클라우드플레어에서 서버를 만들면 아래와 같은 주소가 나옵니다.
   https://writing-partner.무언가.workers.dev
   그 주소를 따옴표 안에 붙여넣고 저장하세요.
   =========================================================== */

window.WB_CONFIG = {
  SERVER: ''
};
